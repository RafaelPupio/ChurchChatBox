# Neon Postgres connection string — the ONLY secret this app needs.
# WhatsApp credentials live in the `church` table, edited via the admin panel.
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"
